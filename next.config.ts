import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Konfigurasi dasar Next.js
  // Jika ada external images, tambah domains di sini:
  // images: {
  //   domains: ['example.com'],
  // },
};

export default nextConfig;
